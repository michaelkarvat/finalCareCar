#include "weight.h"
#include <stdio.h>

float read_weight(void) {
    // Simulate reading weight
    float weight = 75.0; // Example weight value
    printf("Weight: %.2f kg\n", weight);
    return weight;
}
