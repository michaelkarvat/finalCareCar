#ifndef LED_H
#define LED_H

// Initialize the LED (e.g. on PA5).
void LED_init(void);

// Turn the LED off.
void LED_off(void);

// Turn the LED on.
void LED_on(void);

// Toggle the LED.
void LED_toggle(void);

#endif // LED_H
